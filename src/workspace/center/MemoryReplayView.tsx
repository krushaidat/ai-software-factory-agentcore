import { ComingSoon } from './ComingSoon';

export function MemoryReplayView() {
  return (
    <ComingSoon
      iconName="database"
      feature="Feature 2 \u2014 Memory replay"
      description="Scrubbable timeline of AgentCore Memory reads and writes."
    />
  );
}
